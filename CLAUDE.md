# LucasBrain 專案指示

## 報告產生快捷指令

Lucas 在對話中打出以下純文字指令時（不用斜線，直接打在訊息裡），代表要產生對應報告，直接執行對應腳本即可，不用先確認要不要跑：

| 指令 | 對應腳本 | 說明 |
| :--- | :--- | :--- |
| `g Daily_Report` | `.agent/scripts/generate_daily_report.py` | 盤後大盤日報 |
| `g Financial_Screen` | `.agent/scripts/scan_financial_score.py` | 全市場財務指標篩選 |
| `g Invest_Timeline` | `.agent/scripts/generate_invest_timeline.py` | 投資事件行事曆 |
| `g Momentum_Screen` | `.agent/scripts/scan_momentum_score.py` | 全市場動能篩選 |
| `g Weekly_Focus` | `.agent/scripts/generate_weekly_focus.py` | 週度投資決策（估值/均線/乖離率篩出ADD/SELL清單） |
| `g WeeklyReport` | `.agent/scripts/generate_weekly_report.py` | 週報（本週個股/產業更新彙整，narrative格式，與 Weekly_Focus 是分開的兩個功能） |
| `g Stock_Reports {股票代號}` | `.agent/scripts/generate_stock_report.py {股票代號}` | 單一個股深入研報（例如 `g Stock_Reports 2330`） |
| `g Diagnose {股票代號}` | `.agent/scripts/diagnose_model.py {股票代號}` | 建模前診斷：EPS變異來源拆解，建議模型原型與情境軸 |
| `g Model {股票代號}` | `.agent/scripts/build_pnl_model.py {股票代號}` | 損益推估模型：月/季損益表、三情境、滾動12M forward PE |
| `g Backtest {股票代號}` | `.agent/scripts/backtest_model.py {股票代號}` | 模型對帳：模型月營收 vs MOPS 實績，偏離示警 |

執行規則：
1. **只打 `g`（沒有帶報告類型）**：用 AskUserQuestion 工具列出上述 9 個報告類型讓 Lucas 選一個；選到 Stock_Reports / Model / Backtest 的話再追問一次股票代號。選完/回答完才執行。
2. **`g` 後面帶的文字明確對應到某個報告類型**（忽略大小寫、底線/空格差異做寬鬆比對，例如 `g daily report`、`g weekly_focus` 都算數）：直接執行對應腳本，不用再問。
3. **`g Stock_Reports` / `g Model` / `g Backtest` 後面帶了股票代號**：直接執行對應腳本；沒帶代號則追問。
4. **`g` 後面的文字看不出對應到哪個報告類型**：比照規則1跳出選單，不要用猜的執行錯誤的腳本。

執行細節：
- 在 repo 根目錄下用 Bash 工具執行 `python .agent/scripts/{腳本檔名}`。Financial_Screen、Momentum_Screen 這種全市場掃描通常要跑幾分鐘到幾十分鐘，用 `run_in_background: true`；其他幾種較快，前景執行即可。
- 執行完回報產出檔案路徑（.md 與 .pdf）與簡短重點摘要，不用整份貼出來。
- 若該報告會覆蓋今天已產生過的正式報告檔案，執行前用 `git status`/`git diff --stat` 快速確認一下是否有未提交的異動；有的話提醒一聲即可，不用阻擋執行。

### Model / Backtest 專屬規則

- **要替一檔新股票建模時，一律先跑 `g Diagnose`**，用 EPS 變異來源拆解決定模型原型與情境軸，不要憑感覺選結構。診斷結果應寫進 params.yaml 的註解裡。
- `g Model` 會讀取 `31_Models/{代號}{名稱}/params_v*.yaml`（自動取版本號最大者）。若該股尚無參數檔，**不要自己憑空生一份**，先問 Lucas 要不要建，並確認資料來源。
- **`g Model` 執行後一律接著跑一次 `g Backtest`**，因為模型改完必須立刻對帳；回報時把「基期校準是否通過」「有無連續偏離警示」講在前面，EPS 數字講在後面。
- 回報必須包含：三情境 EPS、機率加權、滾動 12M forward PE、以及稽核區塊列出的**推估參數**（那是模型最脆弱處，也是下次法說/私訪的提問清單）。
- **參數只能改 `params_*.yaml`**，`模型摘要_*.md` 與兩個 CSV 都是引擎產出，不要手動編輯。
- 若改版後某年度 EPS 上修超過 20%，先停下來說明是哪個參數造成的，不要直接當成新結論（聯茂 v20→v22 曾在兩天內把 2027 EPS 一路算到 78.56 元才修回 51.99）。
- 詳細設計原則見 `31_Models/README.md`。

### WeeklyReport 專屬說明

`g WeeklyReport` 跟 `g Weekly_Focus` 是**兩個獨立功能**，不要混淆：
- `Weekly_Focus`：依估值評等（ADD/SELL）+ 均線/乖離率技術面篩出的量化選股清單。
- `WeeklyReport`：本週 `10_Stocks`/`20_Garden` 個股與產業**更新內容**的 narrative 週報，格式參考 Lucas 平時手寫的週報（大盤/近期關注事件/個股與產業資訊/結論與交易想法）。

**資料來源與範圍**：
- 週期界定為「本週一到執行當下」（ISO週，非自然7天），輸出檔名沿用執行當下日期：`30_Projects/Weekly_Report/{執行當天日期}_Weekly_Report.md`。
- 【個股資訊 & 產業資訊】段落是唯一自動生成的部分，主要資料來源是 `log.md` 裡本週範圍內的 `## [日期] ingest ...` 條目，逐筆解析裡面 `- **[[頁面名稱]]**：...` 形式的bullet，依 `[[頁面名稱]]` 是否對應到 `10_Stocks/`或`20_Garden/`既有頁面分流。
- **補漏機制**：log.md 每條 ingest 條目的 header 都會列一份「影響」頁面清單；若清單裡的某個頁面在本文卻沒有對應的 bullet（log.md 寫漏了），腳本會回頭找當天檔名前綴相同的 `33_Ingest_Records/{日期}_*_ingest.md`，從其「## 更新內容」段落撈出該頁面的內容當 fallback bullet 補進週報，並加註 `⚠️（補自ingest record，log.md未收錄）` 標記提醒這是補漏內容。`33_Ingest_Records/` 平常不會被讀取，只有偵測到這種 header/本文落差時才會觸發，且僅限 `10_Stocks/`或`20_Garden/`已存在的頁面（例如`大戶籌碼追蹤`這類放在`30_Projects/`底下的頁面目前仍不會被抓進來）。
- 每檔個股只顯示「目前評等/現價/Forward PE」一行狀態列（來自frontmatter）+本週更新bullet，**不會**把 `10_Stocks` 筆記裡完整的「核心論點/歷史筆記」內容整段複製過來——那些筆記本來就是寫給AI讀的詳盡版本、不是給人看的精簡摘要，硬塞進週報只會變成一堆文字牆。
- 若同一批 ingest 的敘述同時提到多檔個股/多個產業頁面（例如「Garden產業筆記」這種一次講六個頁面的彙整句），會自動合併成同一個區塊、不會逐頁重複貼上同一段文字。
- 【大盤】【近期關注事件】【結論與交易想法】三段目前**留白**、由 Lucas 手動填寫——vault 的 ingest 資料裡沒有總體經濟新聞或個人交易水位這類內容，無法自動生成。每檔個股的 `[操作想法]` 也是留白，需 Lucas 補上個人持股/操作看法。
- 只產生 `.md`，不產生 PDF。

**已知限制**：上述補漏機制只能救「header有列、本文漏寫bullet」這種局部缺漏；若某次 ingest 整條都沒有寫入 `log.md`（連 header 都不存在，不符合 SCHEMA.md 步驟6的既有慣例），週報完全無從得知這次 ingest 存在，該次更新仍不會出現在週報中；發現這種缺漏時提醒 Lucas 補寫 `log.md`。

## `ingest temp`：錄音備忘整理

Lucas 打字輸入純文字 `ingest temp`（不用斜線）時，代表要整理 `97_Temp/` 底下的錄音場次備忘，跟一般的 `ingest`（處理 `00_Inbox/`、寫入 `10_Stocks`/`20_Garden` 個股筆記）是**不同的獨立流程**，不要混在一起跑。

**資料結構**：`97_Temp/` 底下每個場次一個資料夾，命名如 `20260915聯茂 元富研究員討論`，內含三個固定檔名的檔案：
- `手稿筆記.md` — Lucas 當下手寫抄錄，內容片段，代表**他自己認為的重點方向**。
- `逐字稿AI重點.md` — 錄音筆內建AI摘要功能產出，**需校正核對**，僅作參考。
- `錄音逐字稿.md` — 錄音筆完整逐字稿，錯字多，**需校正核對**，是主要內容來源。

**執行規則**：
1. 對 `97_Temp/` 底下每一個場次資料夾，讀取三個檔案。
2. 逐字稿類（`逐字稿AI重點.md`、`錄音逐字稿.md`）依 `feedback_ingest_transcript_verification` 規則先做校正核對（數字、產品代號、人名/公司名、同音字誤植等），不可直接照抄轉錄內容。**逐字稿常有代名詞（他/它/這個/對方）指涉對象不清的情況，尤其在談到多家公司比較時，校正時要先判斷這句話講的到底是哪一家公司，不確定就直接問Lucas，不要預設是這次筆記的主角公司。**
3. **跟vault既有累積資料交叉比對**：memo內容整理完後，若某個結論或數字與`10_Stocks/`既有個股筆記、或其他已產出的memo有重大出入（例如同一家公司同一件事，兩個來源說法明顯矛盾），**要直接在對話中問Lucas確認，不能只寫進memo的「待確認事項」就結束**——這跟規則2的「逐字稿校正沒把握」是不同情境（逐字稿校正是懷疑自己聽/認錯字，這裡是新資料跟舊資料兜不起來），兩種情況都要當場問，見規則9。
4. 統整成一份完整memo，**以校正後的「錄音逐字稿」為主要內容骨幹**，用「手稿筆記」的重點方向來決定要著重呈現哪些段落，「逐字稿AI重點」僅作交叉參考、不需整份收錄。
5. **輸出格式（重要）**：若場次內容有明確的「主講/簡報」與「Q&A」兩段，兩段分開處理，不要打散重組成主題式散文：
   - 主講/簡報內容：整理成編號列點（1. 2. 3. ...），依原始講述順序，一點一句話講完一件事。
   - Q&A：**逐題保留「Qn. 問題 / An. 回答」的一問一答配對**，依現場實際發生順序編號，不要合併成分主題的段落摘要。問題可以稍微精簡成一句話，但回答內容要完整保留（校正錯字後），不要摘要壓縮掉細節。
   - 參考格式範例：Lucas 提供的「20260827 台光電小場+Q&A」memo。
   - 若場次沒有清楚的主講/Q&A分段（例如自由討論式的研究員圓桌），才依主題整理成一般段落，不用硬套這個格式。
6. **輸出結尾固定加兩段（不論上面用哪種內容格式）**：
   - `## 重點整理`：依主題分類，把整場內容濃縮成幾段重點摘要（例如：漲價/毛利率、產品組合、產能擴張、稼動率與原物料、技術/競爭議題、財務指引...視場次實際主題而定），讓之後不用重讀整份逐字稿/Q&A就能快速回顧核心資訊。
   - `## 待確認事項`：列出處理過程中發現、但無法100%確認的校正或疑點（見規則9，這區塊是書面備查，不能取代規則9要求的當場詢問）。
7. 輸出檔案：`03_Lucas_Memo/{場次資料夾名稱}.md`（一場次一檔，檔名與場次資料夾同名）。
8. **完成後把該場次資料夾整個搬到 `98_Archives/Temp/` 封存**，`97_Temp/` 只留還沒處理的場次。
9. 回報：產出的memo檔案路徑、每場次簡短重點摘要；若校正逐字稿時發現明顯誤差、身分/數字無法確認、或內容跟vault既有資料有重大出入，**直接在對話中問 Lucas**（見 `feedback_ask_dont_just_log_discrepancies`），不要只寫進檔案的待確認事項就結束。

## `ingest lucas memo`：消化 Lucas Memo 進 vault

Lucas 打字輸入純文字 `ingest lucas memo`（不用斜線）時，代表要把 `03_Lucas_Memo/` 底下已經整理好格式的會議/法說備忘錄，消化進 vault 既有的個股與產業知識庫（`10_Stocks/`、`20_Garden/`）。**邏輯上比照一般 `ingest`（處理 `00_Inbox/`）的做法，但輸入來源與歸檔位置不同，是跟 `ingest`（00_Inbox→10_Stocks/20_Garden）、`ingest temp`（97_Temp錄音場次→03_Lucas_Memo）都不同的第三條獨立流程**，三者不要混在一起跑。

**執行規則**：
1. 讀取 `03_Lucas_Memo/` 底下每一個尚未處理的 memo 檔案。
2. 這些 memo 已經是校正過的整理稿（非原始逐字稿），不需要再做逐字稿層級的錯字校正；但仍要辨識 memo 裡的關鍵個股/產業實體、EPS數字、時間點明確的營運事件等（比照 `SCHEMA.md`「Ingest 標準流程」步驟2 的辨識邏輯）。
3. 依照 `SCHEMA.md`「無損疊加」原則，把 memo 內容疊加寫入對應的 `10_Stocks/{代號}{名稱}.md`（個股相關內容）或 `20_Garden/`（產業/題材相關內容）頁面：
   - 主講內容/Q&A逐題內容，整段或逐條移入對應頁面的「📝 歷史筆記與會議紀要」區塊，**保留逐字/完整細節**（不要只用 memo 本身的「重點整理」段落取代，因為未來報告只讀筆記、不會回頭翻 memo 原文）。
   - memo 的「重點整理」段落可額外濃縮進個股筆記的核心論點/Summary區塊，與逐字內容並存，不互相取代。
   - 新舊資料不同時不可直接覆蓋，必須並列標註；**重大出入（同一件事兩個來源說法明顯矛盾、或 memo 內容跟既有累積資料兜不起來）要直接在對話中問 Lucas，不能只寫進頁面待確認事項就結束**（見 `feedback_ask_dont_just_log_discrepancies`）。
   - 補齊 WikiLinks。
   - memo 的「待確認事項」如果還沒解決，一併移入對應個股筆記的待確認事項，並於回報時直接詢問，不要留到檔案裡就算了。
4. 若 memo 內容找不到對應的既有 `10_Stocks`/`20_Garden` 頁面（例如全新個股/全新產業），**先跟 Lucas 確認再新增頁面**，不要自行創造新分類（比照 `SCHEMA.md`「新股票分類判斷流程」）。
5. **完成後把該 memo 檔案搬到 `98_Archives/Lucas_Memo/` 封存**，`03_Lucas_Memo/` 只留還沒處理的 memo。
6. 更新 `log.md`（追加一行紀錄）與 `33_Ingest_Records/`（產生本次 ingest 摘要記錄），格式比照 `SCHEMA.md`「Ingest 標準八步驟流程」步驟6、7。
7. 回報：更新/新增了哪些頁面、每個頁面的重點異動；若有discrepancy要直接問，不要只寫進檔案。
8. **版本管理**：`10_Stocks/` 變更依既有慣例自動 commit+push；其餘路徑（`20_Garden`、`log.md`、`33_Ingest_Records` 等）依既有慣例需 Lucas 明確講「commit」才會提交。
